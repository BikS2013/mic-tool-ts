# Issues — Pending Items

This file tracks open issues, pending follow-ups, and dependency-vetting decisions for the `mic-tool-ts` project. Pending items are listed first (most critical at the top), followed by completed items, followed by the dependency-vetting log.

## Pending Items

- **Manual compatibility matrix for native focused-input helper** (severity: blocker for helper release validation, not for static review) — The helper is implemented and covered by compile/unit/build checks, but macOS focused-control behavior still needs live verification in TextEdit, Notes, Terminal/iTerm2, Safari and Chrome textareas, Google Docs or another contenteditable editor, VS Code, Cursor, and Slack/Discord using English, Greek, mixed-language, punctuation-heavy, multiline, and long payloads. Use `test_scripts/focused-input-helper-smoke.sh` after `pnpm build` and record which helper method succeeds in each target, whether selected text replacement works, whether undo behaves naturally, and whether the clipboard is preserved.

- **Implement the other seven LLM providers** (severity: feature gap; plan-003 v2) — `openai`, `anthropic`, `google`, `azure-ai-inference`, `ollama`, `litellm`, `openai-compat`. All seven are accepted by config validation but throw `LLMConfigurationError` at refiner construction via `src/llm/factory.ts`. Each needs its own `<provider>.ts` adapter under `src/llm/`, wiring in `factory.ts`, and provider-specific env-var resolution in `resolveProviderConfig()` (`src/config.ts`). The error messages already document the env vars each provider expects (`OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `GOOGLE_API_KEY`, `AZURE_AI_INFERENCE_*`, `OLLAMA_HOST`, `LITELLM_*`, `OPENAI_COMPAT_*`).

- **Port microphone capture to Linux and Windows** (severity: feature gap; investigation-007) — `src/mic/index.ts` currently returns `SoxMicSource` only on `process.platform === "darwin"` and throws `UnsupportedPlatformError` for `linux` and `win32`; tests explicitly assert that non-macOS platforms fail. Investigation in `docs/reference/investigation-007-portability-windows-linux.md` found the STT, LLM, renderer, config, and orchestrator layers are mostly portable, but the project needs Linux/Windows mic backends behind the existing `MicSource` contract. Recommended first path: keep macOS on SoX, add FFmpeg `pulse`/`alsa` support for Linux and FFmpeg `dshow` support for Windows, then add per-OS mocked unit tests and manual smoke scripts under `test_scripts/`.

- **Add `AZURE_OPENAI_API_KEY_EXPIRES_AT` support in code** (severity: medium) — Per CLAUDE.md `<configuration-guide>` the Azure OpenAI key, being a credential that expires, should have an analogous renewal reminder. Currently only `SONIOX_API_KEY_EXPIRES_AT` is wired (`src/config/expiry.ts` → `warnAboutExpiry()` called from `src/main.ts`). The configuration guide documents the variable as "not yet read by code"; this issue tracks closing that gap. Suggested approach: generalize `evaluateExpiry()` / `warnAboutExpiry()` to take a `{ envName, isoDate, renewUrl }` triple and call it once per tracked credential.

- **Live end-to-end run with a real Soniox API key + microphone** (severity: blocker for release, not for review) — AC-5, AC-6, AC-9, AC-11 from `docs/design/refined-request-soniox-mic-transcriber.md` require a real Soniox session, real audio, and macOS mic permission. All static-code preconditions (units A–E, plus turn detection, plus refinement, plus four-tier env chain) are in place; only the live verification is missing.

- **Live end-to-end run with a real ElevenLabs API key + microphone** (severity: blocker for ElevenLabs release validation, not for review) — The ElevenLabs provider is covered by mocked WebSocket unit tests and static verification, but it still needs a real `ELEVENLABS_API_KEY`, accepted Scribe terms in the ElevenLabs dashboard, real microphone audio, and macOS mic permission to validate live partial/final behavior against the production API.

- **Optional shell test wrappers under `test_scripts/`** (severity: minor; AC-14) — `test_scripts/test-help.sh`, `test_scripts/test-version.sh`, `test_scripts/test-missing-key.sh`. The existing `test_scripts/` folder contains TypeScript sanity scripts (`sanity-config.ts`, `sanity-mic.ts`, `sanity-renderer.ts`) but the AC-14 shell wrappers are not yet authored. Each should invoke the built binary, capture stdout/stderr/exit code, and assert.

- **Renderer `dispose()` write-after-end risk** (severity: minor) — `StdoutRenderer.dispose()` writes `\x1b[2K\r` to `process.stdout` even after the orchestrator's teardown has decided to exit. In overwrite mode this is intentional (shell-prompt hygiene) and safe because `process.stdout` is a synchronous TTY write. Documented here so a future change to a non-TTY-aware write path doesn't accidentally write after `process.exit`.

## Completed Items

- **Electron UI settings did not persist across sessions** (resolved 2026-05-20) — Issue: after the first push-to-talk persistence pass, only `hotkeyEnabled` and `hotkey` were saved. Other UI edits such as provider, model, languages, sample rate, endpoint detection, protocol mode, operator defaults, translation policy, and LLM enablement still reset from config/defaults when reopening `mic-tool-ts ui`. Solution: extended `src/ui/settingsStore.ts` to persist the full non-secret user-editable renderer settings subset in `~/.tool-agents/mic-tool-ts/ui-state.json`, kept compatibility with push-to-talk-only state files, restored UI state during `loadRendererSettingsForUi()`, recomputed credential status from the current env/config chain, saved UI state on every accepted renderer update, and added focused store/runtime tests.

- **UI push-to-talk setting did not persist across sessions** (resolved 2026-05-20) — Issue: `hotkeyEnabled` and `hotkey` lived only in Electron main's in-memory `latestSettings`, so reopening `mic-tool-ts ui` reset push-to-talk to the default disabled `Command+'` state. Solution: added `src/ui/settingsStore.ts` to persist non-secret push-to-talk state in `~/.tool-agents/mic-tool-ts/ui-state.json` with a `0700` directory and `0600` file, restored it during `loadRendererSettingsForUi()`, saved it on renderer settings updates, rejected invalid persisted UI state with `InvalidConfigurationError`, and documented the behavior. Added focused store/runtime tests.

- **Electron UI middle and right panes did not scroll reliably** (resolved 2026-05-20) — Issue: the prior overflow CSS still let the active center view and right inspector depend on implicit grid sizing, so content could become unreachable when the window was short even though the body itself stayed fixed. Solution: made the app shell, center content, active view panel, and inspector explicit height-bounded containers; changed active view panels from grid containers to block scroll containers; and extended `test_scripts/verify-ui-bridge.cjs` to launch the packaged renderer in a short window, inject overflow content, and assert that the settings view and inspector both have local vertical scrolling while the document body does not scroll.

- **Native focused-input helper typed only the first few Unicode-event characters** (resolved 2026-05-20) — Issue: when `send --method auto` fell through to the `unicode-events` delivery path, the helper posted synthetic Unicode key down/up events as fast as the loop could create them and then exited immediately. Some focused applications could consume only the first one or two characters even though the helper reported success. Solution: paced Unicode event posting with a short inter-character delay and added a post-delivery flush delay before returning success, so the foreground app has time to process the complete event stream. Verification: Swift helper compile and packaged build pass; live target-app compatibility still belongs to the manual matrix tracked above.

- **Native focused-input helper implemented** (resolved 2026-05-20) — Issue: Plan 014 and the focused helper design specified a macOS-native helper architecture, but production `command input` still used the older `pbcopy` plus System Events paste path. Solution: added `native/macos/input-helper/main.swift`, bundled it as `dist/native/macos/mic-tool-ts-input-helper` during `pnpm build`, added `src/platform/macos/focusedInputHelper.ts` to resolve/spawn the helper and send text over stdin, wired `VoiceAgentProtocolController` to the adapter while preserving `inputWriter` test injection, and added focused adapter/protocol tests plus `test_scripts/focused-input-helper-smoke.sh`. The helper emits structured JSON, keeps transcript text out of argv/stdout/stderr diagnostics, tries AX insertion, Unicode events, then clipboard-preserving key-code paste, and maps failures to existing fail-open protocol warnings.

- **Electron UI sections clipped overflowed content** (resolved 2026-05-20) — Issue: only the transcript timeline and inspector had explicit overflow behavior. Settings, protocol, logs, toolbar, sidebar, and capture controls could clip or become unreachable when content exceeded the window size. Solution: made the major renderer regions bounded scroll containers, added horizontal overflow handling for toolbars/control surfaces, kept body scrolling disabled so scroll is local to sections, documented FR-34 and design section 21, and rebuilt packaged renderer assets.

- **System-wide UI hotkey beeped in foreground apps and did not activate reliably** (resolved 2026-05-20) — Issue: the global push-to-talk path used `uiohook-napi` only as an observer. macOS still delivered `Command+'` to the foreground app, so apps without that menu shortcut could beep, and if the native hook was missing Input Monitoring / Accessibility permission the shortcut appeared not to work outside the Electron window. Solution: `GlobalHotkeyManager` now also registers the configured accelerator through Electron `globalShortcut`, reserving the press path system-wide; `uiohook-napi` remains the release detector because Electron does not expose global keyup. If the native release hook cannot start, the registered shortcut falls back to press-to-toggle and emits a visible warning. Updated README/tool/design docs and tests.

- **UI push-to-talk default hotkey changed to Command+'** (resolved 2026-05-20) — Issue: the system-wide UI push-to-talk feature defaulted to `Command+\`` while the requested default is `Command+'`, and the native global hook needed an explicit apostrophe-to-`Quote` key-code mapping to make that default reliable outside the Electron window. Solution: changed shared and renderer UI defaults to `Command+'`, added apostrophe/quote aliases to the hotkey parser, mapped the parsed apostrophe key to `uiohook-napi`'s `Quote` key, and updated tests and docs. The existing UI hotkey field remains the configuration surface.

- **UI push-to-talk hotkey did not respond reliably** (resolved 2026-05-20) — Issue: the first push-to-talk implementation listened primarily inside the renderer DOM. When focus stayed on a settings input/control, the renderer-side editable-target guard could suppress the configured hotkey, and some accelerator combinations were less reliable when handled only after Chromium dispatched page events. Users could enable the option and press the hotkey without seeing capture start. Solution: moved the primary focused-window hotkey path into Electron main with `webContents.before-input-event`, so the app handles keydown/keyup before focused form controls can swallow them; kept renderer handling only as a fallback; added blur cleanup for held hotkeys; and made `CommandOrControl` match either Command or Control on macOS for the UI feature while preserving exact `Command` / `Control` accelerators. Verification: focused UI hotkey/settings/protocol tests pass.

- **Electron preload bridge failed under sandboxed renderer** (resolved 2026-05-17) — Issue: the UI looked alive but was operationally disconnected: it could show static renderer content, but could not reliably access real configuration, start the command runner, or receive live transcript events. Root cause: `src/ui/preload.ts` compiled to ESM (`dist/ui/preload.js` with `import { contextBridge, ipcRenderer } from "electron"`), while the `BrowserWindow` uses `sandbox: true`. Electron's sandboxed preload environment does not support ESM imports, so the preload bridge could fail before exposing `window.micToolTs`; the renderer then fell into its old demo/local mode and looked useless instead of failing explicitly. Solution: replaced the preload source with `src/ui/preload.cts`, which compiles to `dist/ui/preload.cjs` and uses the sandbox-supported `require("electron")` preload style; updated Electron main to load `preload.cjs`; removed the production demo fallback so a missing preload bridge now shows a visible `Bridge unavailable` error; added `test_scripts/verify-ui-bridge.cjs` to load the packaged renderer with the packaged preload and verify `window.micToolTs`, config loading, and non-secret credential status. Verification output confirmed `hasBridge=true`, provider `soniox`, `SONIOX_API_KEY=configured`, and source `user .env`.

- **Electron UI loaded hard-coded settings instead of resolved runtime config** (resolved 2026-05-17) — Issue: `mic-tool-ts ui` initialized `latestSettings` from `DEFAULT_RENDERER_SETTINGS`, and the renderer loaded those defaults before any call to the real CLI resolver. As a result, the UI could show `SONIOX_API_KEY` as unknown/missing even when it was present in `./.env`, `~/.tool-agents/mic-tool-ts/.env`, or shell env, and `Start Listening` could build explicit CLI-equivalent arguments from stale UI defaults instead of the user's configured provider/model/language/operator state. The production transcript pane itself was already wired to `SessionEvent` partial/final/refined events, but the misleading config state made the app look disconnected from the real command runner. Solution: added `src/ui/runtimeSettings.ts` so Electron main resolves settings through `resolveConfig()` on UI load, applies persisted protocol settings, sends only non-secret status/source metadata to the renderer, and reports typed config errors without inventing fallback values. Added `apiKeySource` to `ResolvedConfig` / `SafeConfigSummary`, refreshed provider credential status on UI settings changes, taught the renderer to consume structured settings-load results and show config errors, and added focused UI runtime settings tests covering local `.env`, LLM-config error inspection, and provider switching.

- **Electron UI controls were static and production transcript pane kept demo data** (resolved 2026-05-16) — Issue: the Electron renderer displayed settings and protocol values as static rows/decorative switches, so the user could not change provider/model/languages/sample rate, enable or disable endpoint/protocol options, or control LLM enablement from the UI. Production mode also initialized with demo transcript rows and did not clear them when the preload bridge was available, making live dictated text hard to distinguish from canned content. Solution: added real settings/protocol forms and clickable inspector switch buttons, added `mic-tool-ts:settings:update` to the preload/Electron main IPC bridge, validates/stores renderer settings in Electron main, converts UI settings to explicit CLI-equivalent args for the next `runMicSession()` start, added positive `--endpoint-detection` so UI can override an env value of `false`, clears demo transcript/event state in production mode, and rebuilt `dist/`. Added focused UI settings tests plus a config test for the new positive endpoint flag; `pnpm typecheck`, `pnpm test`, and `pnpm build` pass.

- **Electron UI command implemented** (resolved 2026-05-16) — Issue: Plan 008 and Plan 009 defined `mic-tool-ts ui`, but the project had no production Electron code, no UI event sink, and no light/dark renderer implementation. Solution: added `electron@^42.1.0`, extracted orchestration into `src/core/sessionRunner.ts`, kept `src/main.ts` as the CLI wrapper, added `UiRenderer` and typed `SessionEvent` delivery, added Electron main/preload/launcher files under `src/ui/`, and added Plan 009 renderer assets with light mode, dark mode, reduced-motion, and reduced-transparency CSS. UI mode starts/stops the shared transcription session from the Electron window and renders human-facing transcript/status text in the UI instead of stdout. Added focused tests for UI renderer events and `ui` dispatch; full test suite, typecheck, build, and audit pass.

- **Focused input System Events error 1002 needed explicit Accessibility remediation** (resolved 2026-05-16) — Issue: macOS can reject the paste step with `osascript is not allowed to send keystrokes. (1002)` even when `command status` shows `input=on` and the section pipeline reaches the input operator. The old warning mentioned Accessibility generically but did not identify the exact System Settings path or the need to enable the app that launched `mic-tool-ts`. Solution: classify `not allowed to send keystrokes` / `(1002)` input failures and emit a targeted remediation telling the user to open System Settings > Privacy & Security > Accessibility, enable Terminal/iTerm2/VS Code/Cursor or whichever app launched the tool, restart that app, and focus the target input before `command send`. Added a protocol regression test and README troubleshooting entry.

- **Focused input delivery failures were invisible without verbose logging** (resolved 2026-05-16) — Issue: `command status` could correctly show `input=on`, but if the macOS focused-input paste failed because the target control was not focused in time or the terminal lacked Accessibility permission, the failure was only logged under `--verbose`. In normal runs this looked like `command input` did not send anything. Solution: focused-input failures now emit a non-fatal stderr warning and a `protocol.warning` event in protocol modes, while preserving fail-open behavior and still withholding `input.sent` unless delivery succeeds. Added protocol tests proving that the focused-input writer receives the final processed output after refinement/translation and that delivery failures emit warnings without terminating the session.

- **Installed runtime did not include the command input operator** (resolved 2026-05-16) — Issue: the TypeScript source had the `command input` implementation, but the installed `mic-tool-ts` command points at `dist/index.js`, and `dist/` was stale from before the focused-input changes. At runtime, the command therefore lacked `--input-default` / `MIC_TOOL_TS_INPUT_DEFAULT`, did not recognize `input` as a protocol operator, did not report `input` in `command status`, and could not emit `input.sent`. Solution: rebuilt the project with `pnpm build`, which refreshed `dist/config.js`, `dist/main.js`, and `dist/protocol/*` to include the current input-operator implementation. Verified the installed command help now exposes `--input-default`, the compiled protocol controller contains `sendToFocusedInput` and `input.sent`, and focused protocol/config/settings tests plus type checking pass.

- **Focused input operator for voice protocol** (resolved 2026-05-16) — Issue: users needed a `command input` operator that sends the final processed section output to the current terminal, GUI field, browser editor, or other focused input control. Solution: added `input` as a persistent protocol operator with `command input` / `command input off`, added `--input-default` / `MIC_TOOL_TS_INPUT_DEFAULT`, included `input` in status reporting and remembered state, emitted `input.sent` on successful focused-input delivery, and implemented a dependency-free macOS delivery path using `pbcopy` plus System Events Command-V. Focused-input failures are fail-open and now emit a non-fatal warning; no transcript or sent text is persisted. Updated README, configuration guide, tool docs, project design, project functions, and plan-007, and added focused tests for config, state persistence, status, and protocol output.

- **Remember voice protocol settings between runs** (resolved 2026-05-16) — Issue: voice-agent operator settings such as `refine`, `translate`, and `clipboard` were reset to defaults on each run, even if the user had changed them orally during the previous session. Solution: added `src/protocol/settingsStore.ts` to persist non-secret protocol settings to `~/.tool-agents/mic-tool-ts/state.json` with `0600` file mode in the `0700` per-user tool folder, added startup restoration that applies saved values only when the corresponding CLI/env default is absent, exposed controller/state-machine settings snapshots, and documented the override behavior. The persisted file contains only operator booleans, `translation_policy`, version, and save timestamp; no API keys, provider endpoints, prompts, transcripts, or processed section text. Added tests for save/load, override precedence, invalid saved state, and shutdown persistence.

- **Voice protocol status command** (resolved 2026-05-16) — Issue: users needed a spoken `command status` command to report current voice-agent settings during a live session. Solution: added `status.reported` to the protocol event model, taught the state machine to treat `command status` as a non-mutating report instead of an unknown operator, report `refine` / `translate` / `clipboard` state, `translation_policy`, and `pending_section`, render a human-readable status line in dictation/hybrid modes, emit JSONL in agent-protocol/hybrid modes, and documented the behavior in README, tool docs, project functions, and project design. Added focused protocol tests for state-machine status, pending-section status, JSONL output, and dictation rendering.

- **Wrapped overwrite transcript rows repeated stale text** (resolved 2026-05-16) — Issue: in TTY `overwrite` mode, a partial transcript longer than the terminal width could wrap across multiple physical rows, but the renderer only used `\r` and single-line padding. Subsequent partial updates could not clear the wrapped rows above the cursor, leaving repeated or stale transcript text as the STT provider emitted new snapshots. Solution: `StdoutRenderer` now tracks the physical row count of the previous overwrite snapshot, clears all previously occupied rows with ANSI cursor movement only on the TTY overwrite path, repaints the latest snapshot once, and resets the row state on final output / refinement / dispose. Added focused renderer tests for wrapped partial-to-partial and partial-to-final repainting; `pnpm typecheck`, focused renderer tests, and the full test suite pass.

- **Repeated interim transcript snapshots rendered as duplicate text** (resolved 2026-05-16) — Issue: realtime STT providers can emit the same partial hypothesis multiple times before a final commit, and Soniox live result frames can also repeat the current finalized prefix while the non-final suffix is still evolving. The previous `SonioxTranscriber` logic appended every final token run unconditionally, so phrases like `Εσύ, ρε παιδί μου... δείξ` could be duplicated inside the same rendered partial many times. Solution: added Soniox finalized-prefix overlap merging, kept renderer-level suppression of identical consecutive partial snapshots, reset the suppression cache on final output / turn boundaries / refined output / dispose, added focused Soniox and renderer regression tests, and updated renderer/provider design and user docs.

- **Voice protocol submit marker changed from `command end` to `command send`** (resolved 2026-05-16) — Issue: the spoken section-submit marker needed to be `command send` instead of `command end`. Solution: changed the default `MIC_TOOL_TS_SECTION_END_PHRASE` value to `command send`; updated protocol/config/orchestrator tests and all protocol documentation references in README, tool docs, configuration guide, project design, project functions, and plan-007.

- **Voice protocol marker wording changed from slash markers to command-prefixed phrases** (resolved 2026-05-16) — Issue: the implemented defaults used slash-style markers (`/command`, `/end`, `/cancel`), but the desired spoken protocol is `command refine|translate|clipboard`, `command send`, and `command cancel`. Solution: changed default marker config to `command`, `command send`, and `command cancel`; updated the state machine so `command <operator>` enables the operator and `command <operator> off` disables it; updated protocol tests, config tests, README, configuration guide, tool docs, project design, project functions, and plan-007.

- **Voice section-processing protocol** (resolved 2026-05-16) — Issue: `plan-007` described an explicit oral protocol for agent communication, but the implementation still only had guard-phrase turn detection. Solution: added the `src/protocol/` layer with command-prefixed marker matching, section state machine, JSONL writer, and controller; added `--interaction-mode`, marker phrase flags, operator defaults, translation policy, and hybrid `--protocol-output`; wired finalized STT text through the protocol controller; added unit coverage for marker matching, state changes, submission, cancellation, JSONL sequencing, operator processing, clipboard events, and shutdown cancellation; updated README, configuration guide, tool docs, project design, and project functions.

- **ElevenLabs transcription provider** (resolved 2026-05-16) — Issue: the tool only supported Soniox for realtime transcription, while the user wanted to know whether ElevenLabs could be supported and used as an alternative. Solution: researched ElevenLabs Scribe Realtime v2, documented the refined request and investigation, added `--stt-provider soniox|elevenlabs`, added provider-specific `ELEVENLABS_API_KEY` and expiry config, introduced `src/transcription/` provider dispatch, implemented `src/elevenlabs/client.ts` over the realtime WebSocket API, updated docs, and added mocked WebSocket tests.

- **Startup readiness message** (resolved 2026-05-16) — Issue: after startup, normal users had no unconditional confirmation that the tool was actively listening unless verbose mode was enabled. Solution: `src/main.ts` now writes `[mic-tool-ts] Ready to listen. Press Control-C to stop the listening tool.` to stderr after Soniox is connected, the microphone is started, signal handlers are installed, and audio is wired. The message is intentionally not written to stdout so transcript output stays pipe-safe. Added a focused orchestrator test and documented the behavior in `README.md`, `docs/design/project-functions.md`, and `docs/design/project-design.md`.

- **Direct OS command was not installed after build** (resolved 2026-05-16) — Issue: `mic-tool-ts --help` failed with `zsh: command not found` after `pnpm run build`, and `pnpm link` failed because pnpm had no configured global bin directory. Solution: confirmed `~/.local/bin` is already on `PATH`, confirmed `dist/index.js` is executable, created `/Users/giorgosmarinos/.local/bin/mic-tool-ts -> /Users/giorgosmarinos/aiwork/coding-platform/mic-tool-ts/dist/index.js`, verified `mic-tool-ts --help`, and updated `README.md` / `docs/tools/mic-tool-ts.md` to document the direct symlink install path instead of relying on `pnpm link`.

- **Project rename to `mic-tool-ts`** (resolved 2026-05-16) — Issue: the project identity, command name, per-user config folder, env-var namespace, and documentation still used the legacy name. Solution: renamed `package.json` to `mic-tool-ts`, changed the installed binary to `mic-tool-ts`, updated `src/config.ts` to load `~/.tool-agents/mic-tool-ts/.env`, moved project-specific env vars to `MIC_TOOL_TS_*`, updated diagnostics to `[mic-tool-ts]`, added `docs/design/plan-005-project-rename-mic-tool-ts.md` and `docs/tools/mic-tool-ts.md`, and synchronized README/design docs/tests/sanity scripts/local agent instructions. The supported user invocation is the direct OS command `mic-tool-ts`.

- **Plan-004 documentation backfill** (resolved 2026-05-16). All five docs synced with the current code state:
  - `docs/design/project-functions.md` extended with FR-12..FR-17 and NFR-8..NFR-10.
  - `docs/design/project-design.md` extended with §12 (turn detection), §13 (LLM refinement), §14 (configuration & env-var chain), §15 (decisions log for plans 002/003/004). Existing §12 ("Open Items Carried Forward") renumbered to §16.
  - `docs/design/configuration-guide.md` created from scratch (covers every flag, every env var, the four-tier chain, expiry tracking, storage recommendations, validation rules summary).
  - `README.md` rewritten from a 5-line placeholder into a real user-facing reference.

- **Plan-003 documentation gaps** (resolved 2026-05-16) — `docs/design/plan-003-llm-refinement.md` was in place but the project-design / project-functions docs hadn't picked up §13 / FR-13 / FR-14 / NFR-10. Closed by the plan-004 docs backfill above.

- **Plan-002 documentation gaps** (resolved 2026-05-16) — `docs/design/plan-002-turn-detection.md` was in place but the project-design / project-functions docs hadn't picked up §12 / FR-12. Closed by the plan-004 docs backfill above.

- **README placeholder** (resolved 2026-05-16) — the prior 5-line placeholder is replaced by a full reference covering prerequisites, install, configuration, usage examples, common errors, and links to the design docs.

- **Configuration guide was missing entirely** (resolved 2026-05-16) — created `docs/design/configuration-guide.md` per the CLAUDE.md `<configuration-guide>` rule.

- **Phase 7 review fix — TDZ on `mic` inside `shutdown` closure** (resolved 2026-05-16, see `docs/reference/code-review-phase-7.md`). `src/main.ts` declared `let mic: MicSource | undefined` AFTER the `shutdown` closure that referenced it. If `transcriber.onError` had fired during the (deferred) shutdown microtask before the original declaration site executed, accessing `mic` would have thrown `ReferenceError`. Moved the declaration above the closure and removed the duplicate at the original site.

## Phase 6 Unit Status

- **Unit D (Stdout renderer)** — implemented 2026-05-15, extended 2026-05-16 with `turnBoundary()` (plan-002), `refined()` (plan-003), duplicate partial suppression, and wrapped-row overwrite cleanup. `src/render/renderer.ts` now contains a full `StdoutRenderer` covering the three output modes (`overwrite`, `append`, `final-only`), the TTY auto-downgrade (`overwrite` → `append` when `isTTY === false`, applied even when the user explicitly chose `overwrite`), shrinking-partial padding via `prevLen`, wrapped partial row tracking via `prevRows`, embedded `\n`/`\r` sanitisation in overwrite mode, empty-text no-op, and an idempotent `dispose()` that terminates any in-progress overwrite line and emits `\x1b[2K\r` only on a real TTY. `turnBoundary()` writes a single `\n` in all three modes. `refined()` defensively commits any in-progress overwrite-mode partial with `\n` before writing `text + "\n\n"`. Constructor accepts an `out?: NodeJS.WritableStream` injection point for tests. `pnpm typecheck` and renderer tests pass.
- **Unit B (Mic source — macOS SoxMicSource)** — implemented 2026-05-15, refactored 2026-05-16 (plan-004) to accept `sampleRate` from config. `src/mic/soxMicSource.ts` spawns `sox` with `[-q, -d, -t, raw, -r, <sampleRate>, -c, 1, -b, 16, -e, signed-integer, -L, -]`. Constructor takes optional `{ verbose?: boolean; sampleRate?: number }`. The orchestrator passes the resolved `config.sampleRate` from Unit A. All other Phase 6 behaviour (ENOENT → `MicNotAvailableError`, coreaudio/permission classification, idempotent `stop()`, etc.) unchanged.
- **Unit C (Soniox client wrapper — `src/soniox/client.ts`)** — implemented 2026-05-16, refactored 2026-05-16 (plan-004) to take `TranscriberOptions { apiKey, model, endpoint, languages, sampleRate, enableEndpointDetection, verbose }`. `SonioxNodeClient` is constructed with `realtime.ws_base_url: endpoint` so the user-configurable endpoint flows through to the SDK. `language_hints` is the resolved array as-is, EXCEPT when `languages === ["auto"]` in which case the session config substitutes `enable_language_identification: true` instead. `sample_rate` is taken from `opts.sampleRate`. `pnpm typecheck` passes.
- **Unit E (Orchestrator / session runner)** — implemented 2026-05-16, extended through plans 002/003/004/007/008/009. `src/core/sessionRunner.ts` owns config resolution, expiry warnings, persisted protocol settings, renderer/protocol stack creation, LLM refiner/translator construction, transcriber and mic lifecycle, signal/abort shutdown, protocol persistence, exit-code mapping, and UI event emission. `src/main.ts` now delegates CLI mode to the shared runner. CLI mode still uses `StdoutRenderer` and stderr diagnostics; UI mode uses `UiRenderer` and typed session events.
- **Unit A (Config & CLI)** — extended 2026-05-16 (plan-004). `src/config.ts` now resolves through `loadEnvChain({ toolName: "mic-tool-ts" })` (in `src/config/envChain.ts`), giving every flag a four-tier fallback. New typed-coercion helpers live in `src/config/parsers.ts`. Expiry warning helper lives in `src/config/expiry.ts`. New CLI flags: `--api-key-expires-at`, `--model`, `--endpoint`, `--sample-rate`, `--no-endpoint-detection`, `--guard-phrase`, `--refine`/`--no-refine`, `--llm-provider`, `--llm-model`. `--language` is now variadic (CLI) AND CSV (env). `ResolvedConfig.language: string` became `ResolvedConfig.languages: string[]`; new fields `model`, `endpoint`, `sampleRate`, `enableEndpointDetection`, `apiKeyExpiresAt?`, `guardPhrase`, `llm: LLMConfig`. `pnpm typecheck` passes.

## Dependency vetting log

All entries follow the format: `<package>@<version pinned> — vetted YYYY-MM-DD — notes`. Vetting consisted of `npm view <pkg> dist-tags` to identify the latest stable major and `pnpm audit --audit-level=high` against a trial install to confirm no HIGH-or-above advisories.

### Runtime dependencies

- `@soniox/node@^2.0.3` — vetted 2026-05-15 — latest stable per `dist-tags.latest`; zero declared transitive deps per investigation; `pnpm audit` clean.
- `commander@^14.0.3` — vetted 2026-05-15 — latest stable per `dist-tags.latest` (v15 is `next`/pre-release only); zero declared transitive deps; `pnpm audit` clean.
- `electron@^42.1.0` — vetted 2026-05-16 — latest stable per `dist-tags.latest` and Electron stable release feed; added for `mic-tool-ts ui`; GitHub/npm advisory search did not surface an unfixed HIGH+ advisory for the candidate version, and `pnpm audit --audit-level=high` is clean after install.
- `uiohook-napi@^1.5.5` — vetted 2026-05-20 — latest stable per `dist-tags.latest`; added for system-wide UI push-to-talk keydown/keyup capture; package ships darwin arm64/x64 prebuilds and depends only on `node-gyp-build`; GitHub/npm advisory search did not surface an unfixed HIGH+ advisory for the candidate version, and `pnpm audit --audit-level=high` is clean after install.
- `ws@^8.20.1` — vetted 2026-05-16 — latest stable per `dist-tags.latest`; added for the ElevenLabs realtime WebSocket client because Node 20 WebSocket availability/stability is not sufficient as the sole runtime surface; `pnpm audit` clean.

### Dev dependencies

- `typescript@^6.0.3` — vetted 2026-05-15 — latest stable per `dist-tags.latest` (published 2026-04-16); `pnpm audit` clean. Required `compilerOptions.types: ["node"]` in `tsconfig.json` because TS 6 no longer auto-includes installed `@types/*` packages.
- `tsx@^4.22.0` — vetted 2026-05-15 — latest stable per `dist-tags.latest`; per CLAUDE.md "fast-moving package" rule we pulled the latest stable major; `pnpm audit` clean (esbuild transitive at 0.28.0, no advisories at HIGH+).
- `@types/node@^20.19.0` — vetted 2026-05-15 — intentionally pinned to the `20.x` line to match `package.json` `engines.node: ">=20.12"`. Newer majors (25.x) exist on npm but typing Node 25 APIs against a Node 20 minimum would risk false-positive compile passes for APIs unavailable at runtime. `pnpm audit` clean.
- `vitest@^4.1.6` — vetted 2026-05-15 — latest stable per `dist-tags.latest`; per CLAUDE.md "fast-moving package" rule we pulled the latest stable major; `pnpm audit` clean (no HIGH+ advisories on the v4 line or its `vite`/`esbuild` transitives).
- `@types/ws@^8.18.1` — vetted 2026-05-16 — latest stable per `dist-tags.latest`; type-only companion for `ws`; `pnpm audit` clean.

### Audit result

`pnpm audit --audit-level=high` reports **0 vulnerabilities** for the resolved dependency tree as of 2026-05-20.
